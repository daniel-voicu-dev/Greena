export default function counter(state = {
  "products" : []
}, {type,payload}) {   
  switch (type) {   
  // case "ADD_PARAGRAPH":
  //   return {...state, ...payload};  
  default:
    return state
  }
}

